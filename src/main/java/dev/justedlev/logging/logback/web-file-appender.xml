<?xml version="1.0" encoding="UTF-8"?>

<included>
    <property name="WEB_LOG_FILE" value="${WEB_LOG_FILE:-${LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}}/web.log}"/>
    <property name="WEB_LOG_FILE_PATTERN" value="${WEB_LOG_FILE_PATTERN:-${FILE_LOG_PATTERN}}"/>
    <appender name="WEB_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>${WEB_LOG_FILE_PATTERN}</pattern>
        </encoder>
        <file>${WEB_LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <cleanHistoryOnStart>false</cleanHistoryOnStart>
            <fileNamePattern>${WEB_LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>0</totalSizeCap>
        </rollingPolicy>
    </appender>
</included>
